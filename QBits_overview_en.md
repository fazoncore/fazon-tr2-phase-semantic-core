# Q-Bits (Quantized Micro-Events) · Overview

Q-Bits are **quantized micro-events** in the phase-semantic space of a system.
They are emitted as telemetry events (JSONL) and can be aggregated into
coarser stability and health indicators.
